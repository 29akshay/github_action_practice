name: Exploring variables and secrets

on: 
    push
    
env: 
    CONTAINER_REGISTRY: docker.io
    DOCKER_USERNAME: dockerhuman999
    IMAGE_NAME: github-action-nginx

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
    - name: Docker Build
      run: echo docker build -t $CONTAINER_REGISTRY/$DOCKER_USSERNAME/$IMAGE_NAME:latest

    - name: Docker login
      env:
        DOCKER_PASSWORD: Akshay@29
      run: echo docker login --username=$DOCKER_USERNAME --password=$DOCKER_PASSWORD

    - name: Docker publish
      run: echo docker push $CONTAINER_REGISTRY/$DOCKER_USERNAME/$IMAGE_NAME:latest

  deploy:
    needs: docker
    runs-on: ubuntu-latest
    steps:
    - name: Docker run
      run: echo docker run -d -p 8000:80 $CONTAINER_REGISTRY/$DOCKER_USERNAME/IAMGE_NAME:latest
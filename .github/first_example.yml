name: My first workflow

on: push

jobs:
    first_job:
        runrs-on: ubantu-latest
        steps:
        - name: checkout repo
          uses: action/checkout@v4

        - name: list file
          run: ls -ltr

        - name: read file
          run: cat README.md
          
            